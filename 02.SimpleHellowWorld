import 'package:flutter/material.dart';

void main() => unApp(MyApp());

class MyApp extends StatelessWidget{
    @override
    Widget build(BuildContext context){
        return Materialapp(
            title:'Welcometo flutter'
            home:Scaffold(
                appBar:AppBar(
                    title:Text('welcome to flutter'),
                ),
                body:Center(
                    child:Text('Hello World'),
                ),
            ),
        );
    }
}